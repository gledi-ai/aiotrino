name: ci

on: [push, pull_request]

jobs:
  run-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          # hatch-vcs derives the version from git tags; need full history
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install libkrb5-dev
          pip install nox nox-uv

      - name: Install uv and set the python version
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: ">=0.11,<0.12"
          python-version: ${{ matrix.python-version }}
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Run linter
        run: nox --session=lint --non-interactive

      - name: Run formatter check
        run: nox --session=format --non-interactive

      - name: Run tests for each supported Python version
        run: nox --session=test --non-interactive --python ${{ matrix.python-version }}
